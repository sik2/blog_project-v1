spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            clientId: NEED_TO_INPUT
custom:
  jwt:
    secretKey: NEED_TO_INPUT
  accessToken:
    expirationSeconds: NEED_TO_INPUT // 기본설정 :  "#{60*20}"